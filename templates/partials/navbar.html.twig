<nav class="navbar navbar-expand-lg navbar-dark bg-primary pt-3 border-secondary">
	<a class="text-light nav-link h3" href="{{ path('home') }}"><i class="text-danger fas fa-dice-d20"></i> AppJdr</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarColor02">
		<ul class="navbar-nav mr-auto ml-3 text-light">
			<li class="nav-item active">
				<a class="nav-link h5" href="#><i class="fa fa-home"></i> Accueil</a>
			</li>
			<li class="nav-item">
				<a class="nav-link h5" href="#"><i class="fas fa-dice-d20"></i> Trouver une partie</a>
			</li>
			<li class="nav-item">
				<a class="nav-link h5" href="#"><i class="fas fa-dice-d20"></i> Créer une partie</a>
			</li>
			<li>
{#				{% for label, messages in app.flashes %}#}
{#							{% for message in messages %}#}
{#								<p class="nav-link h5 text-{{ label }}">{{ message | raw }}</p>#}
{#							{% endfor %}#}
{#				{% endfor %}#}
			</li>
		</ul>
		<ul class="navbar-nav ml-auto">
			{% if app.user %}
				<li class="nav-item dropdown">
					<a class="text-capitalize h5 nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
						test
					</a>
					<div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
						<a class="dropdown-item h5" href="#">Mon compte</a>
						<a class="dropdown-item h5" href="#">Modifier mon compte</a>
						<div class="dropdown-divider"></div>
						<a class="dropdown-item h5" href="{{ path('account_logout') }}">Deconnexion</a>
					</div>
				</li>
			{% else %}
				<li><a class="nav-link h5" href="{{ path('account_registration') }}"><i class="fas fa-user-plus"></i></i> Inscription</a></li>
				<li><a class="nav-link h5" href="{{ path('account_login') }}"><i class="fas fa-sign-in-alt"></i> Connnexion</a></li>
			{% endif %}
		</ul>
	</div>
</nav>
